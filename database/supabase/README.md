# Supabase Database References

이 폴더는 BTS 프로젝트에서 사용하는 Supabase SQL 참고 파일과 migration 후보를 둔다.

- `migrations/0001_create_review_table.sql`: 선수 리뷰 저장용 `review` 테이블 생성 SQL

현재는 수동 적용용 SQL 참고 파일에 가깝다. 운영형 migration 도구를 도입하면 파일명 순서대로 적용되도록 관리한다.
